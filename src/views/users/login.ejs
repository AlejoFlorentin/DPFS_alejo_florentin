<!DOCTYPE html>
<html lang="en">
  <%- include('../partials/head') %>
  <body>
    <%- include('../partials/header') %>

    <main>
      <div
        class="mt-4 d-flex flex-column justify-content-center align-items-center">
        <h1>Iniciar Sesion</h1>

        <form
          class="mt-2 d-flex flex-column justify-content-center w-25"
          id="formLogin"
          action="/usuarios/dataLog"
          method="POST"
          novalidate>
          <div>
            <label class="form-label" for="email">Email</label>
            <input
              class="form-control"
              id="email"
              placeholder="ej: tuemail@email.com"
              type="email"
              name="email"
              autocomplete="off" />
          </div>
          <div class="error text-danger" id="errorEmail"></div>

          <div class="mt-2">
            <label class="form-label" for="password">Contraseña</label>
            <input
              class="form-control mb-1"
              id="password"
              placeholder="ej: tucontraseña"
              type="password"
              name="password"
              id=""
              autocomplete="off" />
          </div>
          <div class="error text-danger" id="errorPassword"></div>
          <div class="mt-2 d-flex flex-row justify-content-between">
            <span class="show-hide" id="show-hide">Show/Hide</span>
            <p><a href="">¿Olvidaste tu contraseña?</a></p>
          </div>

          <div class="form-check mb-3">
            <input
              class="form-check-input"
              type="checkbox"
              id="rememberPassword"
              name="remember" />
            <label class="form-check-label" for="rememberPassword"
              >Recordarme</label
            >
          </div>

          <input class="mb-2" type="submit" value="Inicia Sesion" />

          <p class="text-center">
            ¿No tenés cuenta? <a href="/usuarios/registro">Crear cuenta</a>
          </p>
        </form>
      </div>
    </main>

    <%- include('../partials/footer') %> <%- include('../partials/wsp') %>
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <script src="/js/login.js"></script>
    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
      integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
      crossorigin="anonymous"></script>
  </body>
</html>
